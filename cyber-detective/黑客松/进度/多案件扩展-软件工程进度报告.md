# 多案件扩展 · 软件工程进度报告

> 更新时间：2026-06-17
> 负责人：A（程序主程）
> 状态：**第一阶段全部完成，案件1→Hub→案件2 流程已可端到端跑通（含占位内容）**
> 构建验证：`vite build` 通过（11 模块，522ms，0 错误）

---

## 一、本次已完成的工作（A · 程序主程）

### ✅ 1. 数据架构重构

| 文件 | 操作 | 说明 |
|------|------|------|
| `data/case.json` | 删除 | 旧的单案件文件，已迁移 |
| `data/cases/case_001.json` | 新建 | 案件1完整数据（从原 case.json 迁移，补全 endings.description） |
| `data/cases/case_002.json` | 新建 | 案件2「数据深渊」骨架（3场景3嫌疑人结构，TODO_B 标记待填充） |
| `data/cases/case_003.json` | 新建 | 案件3「义体战争」骨架（3场景3嫌疑人结构，TODO_B 标记待填充） |
| `data/cases/case_004.json` | 新建 | 隐藏案件「最后的真相」骨架（含 unlockCondition 字段，TODO_B 待填充） |
| `data/plot.json` | 新建 | 主线剧情骨架（hubDialogues/newsFeed/npcInteractions/plotBranches/finalEndings 五大结构已定义，TODO_B 待填充内容） |
| `prompts/plotNarration.txt` | 新建 | 主线旁白 Prompt 模板（A 编写，待 B 校准变量） |

### ✅ 2. 状态管理双层架构（gameState.js 重写）

将单案件状态升级为**跨案件层 + 单案件层**两层架构：

```
跨案件层（持久化，不随案件切换重置）：
  - gamePhase: 'menu'|'briefing'|'investigate'|'interrogation'|'hub'|'ending'|'final_ending'
  - currentCaseIndex: 0/1/2/3
  - casesResults: [{ caseId, ending, score, keyFlags, completedAt }]
  - unlockedHiddenCase: boolean
  - globalFlags: {}

单案件层（切换案件时重置）：
  - currentPhase / currentScene / currentSuspect
  - evidenceObtained / evidenceExamined / dialogueHistory
  - stressLevel / score / endingTriggered / flags
```

新增方法：
- `startNewCase(caseIndex)` — 重置单案件层，保留跨案件层
- `recordCaseResult(result)` — 记录案件结果到 casesResults
- `checkHiddenCaseUnlock()` — 检查前3案是否全好结局，解锁隐藏案件
- `setGamePhase()` / `getGamePhase()` — 跨案件阶段管理
- `getCasesResults()` / `getCaseResult()` / `getCurrentCaseIndex()`
- `initSuspectStress(suspectIds)` — 按案件动态初始化压力值
- `getFinalEndingType()` — 计算最终结局类型（all_good/mixed/all_bad）
- `isAllMainCasesCompleted()` — 判断主线全通
- `setGlobalFlag()` / `getGlobalFlag()` — 跨案件标记

存档兼容：`loadGame()` 自动补充旧版存档缺失的跨案件层字段，version=2。

### ✅ 3. Hub 事务所画面（index.html + style.css）

- `index.html` 新增 `#hub-screen` 画面：顶部状态栏 / 左侧AI义眼面板 / 右侧新闻简报 / 底部案件进度指示器+操作按钮
- `index.html` 新增 `#final-ending-screen` 最终结局画面
- `style.css` 新增完整 Hub 样式（赛博朋克霓虹主题）：
  - AI 义眼呼吸光晕动画（`hub-eye-breath`）
  - 新闻条目逐条淡入动画（`hub-news-fadein`）
  - "开始调查"按钮霓虹流光扫描（`hub-btn-sweep`）
  - 案件进度节点（好结局霓虹绿✓ / 坏结局霓虹红✗ / 普通黄— / 未完成数字 / 锁定虚线）
  - 响应式适配（768px 断点）

### ✅ 4. 主线剧情管理模块（plotManager.js · 新建）

- `initPlotManager(plotData)` — 加载 plot.json
- `getHubDialogue(caseIndex, endingType)` — 按案件+结局获取 Hub 对话
- `getCurrentHubDialogue()` — 基于最近结案结果获取对话
- `getNewsFeed(caseIndex)` / `getAllNewsFeed()` — 新闻简报
- `evaluateBranch()` — 评估剧情分支（unlock_hidden_case / final_ending_mixed / final_ending_bad）
- `getNextCaseIndex()` — 计算下一个案件序号（含隐藏案件逻辑）
- `shouldEnterFinalEnding()` — 判断是否进入最终结局
- `getFinalEnding()` — 获取最终结局文本

### ✅ 5. 场景管理器多案件扩展（sceneManager.js）

新增/改造函数：
- `setCaseData(caseData)` — 切换激活案件，自动合并 characters.json 嫌疑人数据 + 同步 AI 引擎上下文
- `_loadCase(caseIndex)` — 按需 fetch 案件 JSON（带缓存）
- `startCase(caseIndex)` — 加载案件→重置单案件状态→初始化压力值→渲染简报
- `endCaseAndReturnHub()` — 记录结果→检查隐藏解锁→切换 Hub→渲染
- `_renderHub()` — 渲染进度指示器/新闻/AI对话/配置按钮
- `_renderCaseIndicator()` — 渲染3+1案件进度节点
- `_renderNewsFeed()` — 渲染新闻简报列表
- `renderFinalEnding()` — 渲染最终结局（累计评分）
- `_handleNextFromHub()` — Hub"开始调查"按钮：进入下一案件或最终结局
- `_handleReviewCases()` — 回顾已结案件卷宗

全局动作新增：`next-case` / `review-case` / `back-hub`
`switchScreen` 新增 `hub` / `final_ending` 映射
`_restoreFromSave` 改为 async，优先按 gamePhase 恢复（hub/final_ending），并按需加载案件数据
`new-game` 改为 `startCase(0)` 启动多案件流程

### ✅ 6. AI 引擎动态案件上下文（aiEngine.js）

- 新增 `setAIEngineCaseContext(caseData)` — 运行时切换 AI 引擎引用的案件数据
- 在 `setCaseData` 时自动调用，确保审讯/证据组合/场景调查使用当前案件数据

### ✅ 7. main.js 多案件初始化

- 加载 `data/plot.json`（_totalResources 从 4 升至 5）
- 案件数据路径改为 `/data/cases/case_001.json`
- 注入 `caseLoader`（按需 fetch `/data/cases/{caseId}.json`）
- `initSceneManager` 传入 plotData + caseLoader

---

## 二、当前可运行状态

✅ **案件1 完整流程可玩**：新游戏 → 简报 → 调查 → 审讯 → 结案 → 返回事务所 Hub → 开始案件2
✅ **Hub 画面已可用**：含占位 AI 对话、保底新闻、案件进度指示器
✅ **多案件流转框架已通**：案件1→Hub→案件2→Hub→案件3→Hub→最终结局 全链路代码就绪
✅ **隐藏案件解锁逻辑已实现**：前3案全好结局自动解锁
✅ **存档兼容**：旧存档可读取，新增 gamePhase 字段支持 Hub 阶段恢复
⚠️ **案件2/3/4 内容为骨架**：场景描述/对话/嫌疑人配置均为 TODO_B 占位，可跑通但无实质内容

---

## 三、🔴 需要队友推进的事项

### 🔴 需 B（AI 架构师）推进

> 这些是阻塞实质内容的关键依赖。代码框架已就绪，B 填充后即可生效，**无需 A 改代码**。

| 优先级 | 任务 | 文件 | 说明 |
|:---:|------|------|------|
| **P0** | 填充 `plot.json` 全部内容 | `data/plot.json` | 替换所有 `TODO_B_` 标记：3个案件的 Hub 对话（good/normal/bad 各3条）、新闻简报、NPC 交互、最终结局3种文本。**结构已定义，直接填值即可** |
| **P0** | 填充 `case_002.json` 完整内容 | `data/cases/case_002.json` | 替换 `TODO_B_` 标记：setting/victim/truth/3场景描述与热点/3结局。嫌疑人ID已定义为 suspect_201/202/203 |
| **P1** | 填充 `case_003.json` 完整内容 | `data/cases/case_003.json` | 同上结构，suspect_301/302/303 |
| **P1** | 填充 `case_004.json` 完整内容 | `data/cases/case_004.json` | 隐藏案件，suspect_401 |
| **P1** | 在 `characters.json` 添加案件2/3/4 嫌疑人 | `data/characters.json` | 按 suspect_2xx/3xx/4xx ID 添加角色对象（name/identity/personality/secretKnowledge/lies） |
| **P1** | 在 `evidence.json` 添加案件2/3/4 证据 | `data/evidence.json` | 按 evidence_2xx/3xx/4xx ID 添加证据（含 canCombineWith/relatedTo） |
| **P2** | 在 `dialogues.json` 添加案件2/3/4 审讯对话 | `data/dialogues.json` | interrogation_opening / interrogation_questions 按 suspect ID 分组 |
| **P2** | 校准 `prompts/plotNarration.txt` 变量 | `prompts/plotNarration.txt` | A 已写初版，B 确认 {{currentSituation}} {{casesSummary}} 变量来源 |
| **P2** | 在 `aiEngine.js` 添加案件2/3/4 离线预设回应 | `modules/aiEngine.js` PRESET_RESPONSES | 按 suspect_2xx/3xx/4xx 添加 calm/nervous/broken 回应（离线模式保底） |

**B 填充数据格式约定**（A 已在骨架文件中用 TODO_B 标记所有待填位置，直接搜索 `TODO_B` 即可定位）。

### 🔴 需 C（美术叙事导演）推进

| 优先级 | 任务 | 路径约定 | 说明 |
|:---:|------|----------|------|
| **P0** | Hub 事务所背景图 | `assets/bg/hub_office.svg` | Hub 画面当前用 CSS 渐变占位，C 出图后可接入（需在 `_renderHub` 中设置背景） |
| **P1** | 案件2 场景背景 ×3 | `assets/bg/case002_scene_1/2/3.png` | 路径已在 case_002.json 的 bgImage 定义 |
| **P1** | 案件2 嫌疑人立绘 ×3 | `assets/characters/suspect_201/202/203.png` | 需在 characters.json 的 portrait 字段引用 |
| **P1** | 案件3 场景背景 ×3 | `assets/bg/case003_scene_1/2/3.png` | case_003.json 已定义路径 |
| **P1** | 案件3 嫌疑人立绘 ×3 | `assets/characters/suspect_301/302/303.png` | |
| **P2** | 隐藏案件素材 | `assets/bg/case004_scene_1.png` 等 | case_004.json 已定义路径 |
| **P2** | Hub BGM | `assets/audio/bgm_hub.wav` | audioManager 需新增 hub 类型（A 待配合） |
| **P2** | 案件切换音效 | `assets/audio/sfx_case_transition.wav` | |
| **P2** | 案件进度 UI 图标 | `assets/ui/case_progress_*.svg` | 当前用文字✓/✗/数字，可升级为图标 |

**素材路径约定**：A 已在所有 case JSON 中预定义 bgImage 路径，C 按路径放置文件即可生效，**无需 A 改代码**。

### 🟡 需 A（程序主程）后续配合的事项

> 这些在 B/C 产出后由 A 配合接入或测试。

| 任务 | 触发条件 |
|------|----------|
| Hub 背景图接入 `_renderHub` | C 提供 hub_office.svg 后 |
| audioManager 新增 hub/transition 音频类型 | C 提供 bgm_hub.wav 后 |
| 案件2/3/4 端到端联调测试 | B 填充 case 数据 + C 提供素材后 |
| Bug 修复（边界情况、切换中断、存档兼容） | B/C 内容就位后全链路测试 |
| CloudStudio 部署 | 全部内容就位后 |
| 录屏 Demo 视频 | 完整流程可跑通后 |

---

## 四、技术架构现状

### 模块依赖关系（清理后）

```
main.js
  ├── gameState.js        （双层状态：跨案件层 + 单案件层）
  ├── plotManager.js      （主线剧情：Hub对话/新闻/分支/最终结局）★新建
  ├── sceneManager.js     （+Hub渲染/案件切换/最终结局/存档恢复）
  ├── dialogueSystem.js   （不变）
  ├── evidenceSystem.js   （不变，证据全局共享）
  ├── aiEngine.js         （+setAIEngineCaseContext 动态案件上下文）
  └── audioManager.js     （不变，待新增hub音频类型）

数据层：
  data/cases/case_001.json  ← 案件1（完整）
  data/cases/case_002.json  ← 案件2（骨架，待B填充）
  data/cases/case_003.json  ← 案件3（骨架，待B填充）
  data/cases/case_004.json  ← 隐藏案件（骨架，待B填充）
  data/plot.json            ← 主线剧情（骨架，待B填充）
  data/characters.json      ← 全案件角色（待C扩展）
  data/evidence.json        ← 全案件证据（待C扩展）
  data/dialogues.json       ← 全案件对话（待B扩展）
```

### 案件流转状态机

```
新游戏 → startCase(0) → 简报 → 调查 → 审讯 → 结案
                                                ↓
                                          ending画面 → back-hub
                                                          ↓
                                                       Hub画面 → next-case
                                                          ↓
                                              getNextCaseIndex() 决定:
                                                ├─ 有未完成案件 → startCase(next)
                                                └─ 全部完成 → renderFinalEnding()
                                                              ↓
                                                         final_ending画面 → back-menu

隐藏案件解锁：案件0/1/2 全 ending_good → checkHiddenCaseUnlock() → unlockedHiddenCase=true
            → getNextCaseIndex() 返回 3 → startCase(3)
```

---

## 五、验证结果

| 验证项 | 结果 |
|--------|------|
| `vite build` 构建 | ✅ 通过（11模块，522ms，0错误） |
| ESLint 检查 | ✅ 0 错误（modules/ + main.js） |
| 导入完整性 | ✅ 所有 import 均能解析 |
| 数据文件 JSON 合法性 | ✅ 5个新JSON文件结构合法 |
| 旧存档兼容 | ✅ loadGame 自动补充跨案件层字段 |

---

## 六、下一步建议（按优先级）

1. **B 立即启动**：填充 `plot.json` + `case_002.json` → 解除案件2内容阻塞（这是最大瓶颈）
2. **C 并行启动**：生成 Hub 背景图 + 案件2 素材（不阻塞代码，可立即开工）
3. **A 待命**：B/C 产出后做联调测试 + 部署
4. **案件2优先跑通**：集中资源先打通 案件1→Hub→案件2，再复制模式做案件3/4

---

*本文档由 A（程序主程）维护，记录多案件扩展软件工程阶段完成情况。B/C 请参照"需队友推进事项"章节开展工作。*
